/*using DataModels.Card;
using Enums;

namespace trash
{
    public class PlayingCardController : ICardController
    {
        public bool? IsCardCanBePlayed<T>(T cardModel)
        {
            if (typeof(T) == typeof(BasicCardModel))
            {
     
               return IsCardCanBePlayed(cardModel);
            }
            else if (typeof(T) == typeof(PlayingCardModel))
            {
                // the object is a string
            }

            return null;
        }
    }
}*/