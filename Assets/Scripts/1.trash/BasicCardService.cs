/*using DataModels.Card;

namespace trash.Services
{
    public abstract class BasicCardService
    {
        public  virtual  bool? IsCardBlocked(BasicCardModel cardModel)
        {
            if (cardModel.CardBlockers.Count == 0)
                return true;
            if (cardModel.CardBlockers.Count > 0)
                return false;
            
            return null;
        }
    }
}*/